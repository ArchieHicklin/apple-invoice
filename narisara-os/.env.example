# Optional — enables AI-powered natural-language event parsing in the Month view.
# Without a key, Narisara OS falls back to its built-in date parser (works offline).
# Uses a cheap model (gpt-4o-mini) for parsing only.
VITE_OPENAI_API_KEY=
